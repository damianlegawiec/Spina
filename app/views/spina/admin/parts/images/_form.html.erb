<div class="mt-6" data-controller="media-picker">
  <label for="price" class="block text-sm leading-5 font-medium text-gray-700"><%= f.object.title %></label>

  <%= f.hidden_field :signed_blob_id, placeholder: "Signed blob ID", data: {target: "media-picker.signedBlobId"} %>
  <%= f.hidden_field :filename, placeholder: "Filename", data: {target: "media-picker.filename"} %>
  <%= f.hidden_field :image_id, placeholder: "Image ID", data: {target: "media-picker.imageId"} %>

  <button type="button" data-action="media-picker#open" class="relative mt-1">

    <div class="w-36 h-36 bg-transparent border-2 border-dashed border-gray-300 rounded-lg flex items-center flex-col justify-center">
      <svg class="mx-auto h-12 w-12 text-gray-400" stroke="currentColor" fill="none" viewBox="0 0 48 48">
        <path d="M28 8H12a4 4 0 00-4 4v20m32-12v8m0 0v8a4 4 0 01-4 4H12a4 4 0 01-4-4v-4m32-4l-3.172-3.172a4 4 0 00-5.656 0L28 28M8 32l9.172-9.172a4 4 0 015.656 0L28 28m0 0l4 4m4-24h8m-4-4v8m-12 4h.02" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
      </svg>
      <div class="text-gray-400 font-medium text-sm">Choose image</div>
    </div>

    <div class="border absolute inset-0 w-36 h-36 bg-gray-100 rounded-lg shadow-md overflow-hidden" data-target="media-picker.thumbnail">
      <% if f.object.signed_blob_id.present? && f.object.spina_image.present? %>
        <%= image_tag thumbnail_url(f.object.spina_image), class: 'object-contain h-36 w-full' %>
      <% end %>
    </div>

  </button>

  <%= f.text_field :alt, placeholder: "Alt text", class: "form-input w-full max-w-xs mt-3 shadow-sm text-sm block", data: {target: "media-picker.alt"} %>

  <button data-action="media-picker#removeImage" type="button">Remove image</button>

  <div data-controller="modal" data-target="media-picker.modal" class="hidden fixed inset-0 bg-gray-700 bg-opacity-50 z-50 flex items-center justify-center p-6">
    <button type="button" class="fixed inset-0 cursor-default" tabindex="-1" data-action="modal#close"></button>
    <div class="bg-white rounded-lg w-full max-w-4xl p-6 shadow-lg relative max-h-full flex flex-col">
      <div class="flex justify-between items-center mb-10">
        <div class="text-lg font-medium text-gray-700">Choose image</div>
        <button type="button" class="text-2xl" data-action="modal#close">&times;</button>
      </div>

      <!-- Media picker grid -->
      <div 
        class="flex-1 overflow-scroll grid sm:grid-cols-4 md:grid-cols-5 gap-3 auto-rows-min"
        data-target="media-picker.grid" 
        data-images-path="<%= spina.admin_media_picker_images_path %>">
      </div>
    </div>
  </div>

</div>
