// Append new data
$("<%=j render partial: 'spina/admin/images/image', collection: @images %>").appendTo($(".infinite-scroll"));

// Update pagination link
$('.infinite-pagination').html("<%= j link_to_next_page @images, 'Next', remote: true, data: {infinite_scroll_target: 'link'} %>");

$('.infinite-scroll .item:not(.ui-draggable)').draggable({revert: 'invalid', disabled: true});
