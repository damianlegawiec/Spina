<%= turbo_stream.remove @image %>

<% if @image.media_folder %>
  <%= turbo_stream.update @image.media_folder, partial: @image.media_folder.to_partial_path, locals: {media_folder: @image.media_folder} %>
<% end %>
